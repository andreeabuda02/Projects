import React from "react";

export const mainDivStyle: React.CSSProperties = {
    backgroundImage: "url(/MenuInterface.png)", 
    display: "block", 
    height: "100vh",
    backgroundSize: "cover",
    backgroundPosition: "center",
    
}

export const parentDivStyle: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    
}

export const listDivStyle: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
    paddingLeft: 100,
}

export const buttonsDivStyle: React.CSSProperties = {
    display: "flex",
    justifyContent: "space-between",
}

export const optionsDivStyle: React.CSSProperties = {
    display: "flex",
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
}