import React from "react";

export const mainDivStyle: React.CSSProperties = {
    backgroundImage: "url(/SelectRestaurant.png)", 
    display: "block", 
    height: "100vh",
    backgroundSize: "cover",
    backgroundPosition: "center",
    
}

export const parentDivStyle: React.CSSProperties = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
    justifyContent: "center",
}

export const buttonsDivStyle: React.CSSProperties = {
    display: "flex",
    justifyContent: "space-between",
}

export const restaurantNameStyle: React.CSSProperties = {
    display: "flex",
    fontWeight: "bold"
};